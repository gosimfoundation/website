---
import { getLangFromUrl, useTranslations, localizedPath } from '../i18n/utils';
import LanguagePicker from './LanguagePicker.astro';

interface Props {
  /** Full-bleed hero pages start transparent; content pages start solid. */
  transparent?: boolean;
}
const { transparent = false } = Astro.props;

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const links = [
  { href: localizedPath('/blog', lang), label: t('nav.stories') },
  { href: localizedPath('/events', lang), label: t('nav.events') },
  { href: localizedPath('/newsletter', lang), label: t('nav.newsletter') },
];
---

<header class={`site-header fixed inset-x-0 top-0 z-20 ${transparent ? '' : 'is-solid'}`}>
  <div class="flex min-h-[78px] items-center justify-between gap-6 px-5 py-4 sm:px-8 lg:px-[72px]">
    <a href={localizedPath('/', lang)} class="shrink-0" aria-label="GOSIM home">
      <img src="/logo.svg" alt="GOSIM" width="112" height="32" class="w-28" />
    </a>

    <nav class="flex items-center gap-4 text-sm sm:gap-6 lg:gap-8">
      {links.map((l) => (
        <a href={l.href} class="hidden transition-colors hover:text-teal sm:inline">{l.label}</a>
      ))}
      <LanguagePicker />
      <a
        href={localizedPath('/events', lang)}
        class="inline-flex min-h-[42px] items-center rounded-full border border-current px-[18px] py-3 transition-colors hover:bg-ink hover:text-paper"
      >
        {t('nav.projects')}
      </a>
    </nav>
  </div>
</header>
